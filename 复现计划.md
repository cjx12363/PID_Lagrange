# PID-Lagrangian 安全强化学习复现计划 (v2)

## 零、环境信息（开新窗口第一件事）

- **Conda 环境**：`PID-L`，路径 `E:\miniconda\envs\PID-L\python.exe`
- **Python**：该环境的 python.exe
- **已安装依赖**：torch 2.12.0、stable-baselines3 2.8.0、gymnasium 1.2.3、pandas 3.0.3、numpy 2.4.6、matplotlib
- **EV2Gym**：已下载到 `E:\cjx12363\PID\EV2Gym\`，18.8MB，含完整数据
- **参考文献**：已下载到 `E:\cjx12363\PID\papers\`（4篇PDF）
- **所有命令前缀**：`& "E:\miniconda\envs\PID-L\python.exe"` 或先 `conda activate PID-L`

---

## 一、技术路线与选型决策

### 最终选型：EV2Gym (环境+数据) + 自写 SAC-PID (算法) + PyTorch

| 候选方案 | 评估结果 |
|---------|---------|
| EV2Gym (217 stars) | 选中：真实荷兰电价数据(ENTSO-E 2015-2024)、ELAAD充电行为数据、光伏/负荷数据、SB3集成、Gurobi/MPC基线、IEEE TITS论文背书 |
| Chargym (145 stars) | 不选：假设电网无限供电，恰与你核心约束(变压器容量限制)相反 |
| Chargax (9 stars) | 不选：JAX加速确快，但太新、数据来源不明、生态不成熟，debug成本高于训练时间收益 |
| 从零实现 | 不选：无必要重复造轮子，EV2Gym已提供完整环境和数据 |

### 为什么 PyTorch 而非 JAX

10个充电桩的仿真不是性能瓶颈。SAC是off-policy，环境交互和网络更新解耦。
完整训练预估30-60分钟，这点时间远小于debug一个不成熟JAX框架的时间。

## 二、算法核心——PID-Lagrangian乘子更新

来自 Stooke et al. (ICML 2020), Algorithm 2：

```python
class PIDLagrangianUpdater:
    def __init__(self, K_P, K_I, K_D, d):
        self.K_P = K_P      # 比例系数
        self.K_I = K_I      # 积分系数
        self.K_D = K_D      # 微分系数
        self.d = d          # 安全阈值
        self.I = 0.0        # 积分累积项
        self.J_C_prev = 0.0 # 上一轮累积代价

    def update(self, J_C):
        # 误差
        Delta = J_C - self.d
        # 微分项: 仅在成本上升时激活
        partial = max(J_C - self.J_C_prev, 0)
        # 积分项: 累积误差
        self.I = max(self.I + Delta, 0)
        # PID 合成
        lam = self.K_P * Delta + self.K_I * self.I + self.K_D * partial
        lam = max(lam, 0)  # 非负投影
        self.J_C_prev = J_C
        return lam
```

## 三、核心公式

### 3.1 PID乘子更新

lambda_{k+1} = (K_P * Delta_k + K_I * I_k + K_D * partial_k)+

- Delta_k = J_C(k) - d （违规误差）
- I_k = (I_{k-1} + Delta_k)+ （积分累积）
- partial_k = (J_C(k) - J_C(k-1))+ （微分项，仅惩罚增加）

### 3.2 目标重缩放（论文关键技巧）

Actor loss: L = (1/(1+lambda)) * (lambda * J_C_gradient - J_R_gradient)

### 3.3 网络架构

- Actor: state -> [256, ReLU] -> [256, ReLU] -> (mu, log_sigma)
- Reward Critic (双Q): state+action -> [256, ReLU] -> [256, ReLU] -> Q_R
- Safety Critic (双Q): state+action -> [256, ReLU] -> [256, ReLU] -> Q_C
- 共 6 个网络（2个Critic双Q + 2个Safety Critic双Q + 1 Actor + 4 Target）

### 3.4 Loss函数

Reward Critic: L_R = MSE(Q_R(s,a), r + gamma*(min Q_R_target - alpha*log_pi))
Safety Critic: L_C = MSE(Q_C(s,a), c + gamma*min Q_C_target)
Actor: L_actor = E[alpha*log_pi - (min Q_R - lambda*min Q_C)]

## 四、环境——基于 EV2Gym 修改

### 4.1 需要修改的部分

| 文件 | 修改内容 |
|------|---------|
| `ev2gym/rl_agent/cost.py` | 加入变压器过载代价：ReLU((P_base + sum P_i) / P_rated - 1) |
| `ev2gym/rl_agent/state.py` | 增加变压器负载率和裕度指标到状态空间 |
| `ev2gym/example_config_files/` | 新建一个适配PID-Lagrangian的配置文件 |

### 4.2 状态空间

| 特征 | 来源 |
|------|------|
| 变压器负载率 + 裕度 | 修改 state.py 加入 |
| 时间编码 sin/cos | EV2Gym 已有 |
| 电价 + 历史窗口 | EV2Gym 已有 (Netherlands_day-ahead) |
| EV SOC/目标SOC/剩余时间/容量/功率 | EV2Gym 已有 |
| 上一时刻动作 | EV2Gym 已有 |

### 4.3 代价函数（核心修改）

c_t = ReLU((P_base(t) + sum_i P_i(t)) / P_rated - 1)
安全约束：E_pi[sum_t c_t] <= d

## 五、超参数

| 参数 | 值 |
|------|-----|
| 学习率 | 1e-4 |
| 折扣因子 gamma | 0.99 |
| 温度 alpha | 自动调节 |
| 软更新 tau | 0.005 |
| 隐藏层 | 256 x 2, ReLU |
| 经验池 | 1e6 |
| 批量大小 | 256 |
| K_P | 0.10 |
| K_I | 0.01 |
| K_D | 0.05 |
| 安全阈值 d | 25.0 |
| 成本缩放 | 1/10 |

## 六、训练流程

```
每个episode:
  for t in range(T):
    s_t -> Actor -> a_t (重参数化采样)
    执行 a_t, 得到 r_t, c_t (过载代价), s_{t+1}
    replay_buffer.add(s_t, a_t, r_t, c_t, s_{t+1})

  J_C = sum(c_t)  # 非折扣episodic累积代价
  lambda = pid_updater.update(J_C)

  for _ in range(G):  # 梯度更新
    batch = replay_buffer.sample(B)
    更新 Safety Critic (min L_C)
    更新 Reward Critic (min L_R)
    更新 Actor (min L_actor，使用 1/(1+lambda) 重缩放)
    软更新目标网络
```

## 七、对比基线

| 算法 | 说明 |
|------|------|
| PID-Lagrangian SAC (ours) | K_P=0.10, K_I=0.01, K_D=0.05 |
| Lagrangian SAC | K_P=K_D=0，退化为纯积分控制 |
| Unconstrained SAC | 无安全约束，仅最大化奖励 |
| Gurobi 最优解 (EV2Gym自带) | 数学优化上界 |
| 启发式 (EV2Gym自带) | 规则基线 |

## 八、评价指标

1. 累积奖励曲线（episodic return）
2. 累积代价曲线（应 <= d）
3. lambda 收敛曲线（验证PID震荡抑制）
4. 帕累托前沿图（收益 vs 代价散点）
5. 变压器过载率（测试阶段）

## 九、项目文件结构

```
PID/
|-- EV2Gym/                     # 环境 + 数据
|   |-- ev2gym/
|   |   |-- data/               # 荷兰电价、光伏、负荷、EV分布
|   |   |-- rl_agent/           # 状态/动作/奖励/代价定义
|   |   |-- baselines/          # Gurobi/MPC/启发式基线
|   |   +-- example_config_files/
|   +-- train_stable_baselines.py
|-- agent/                      # 我们的算法实现
|   |-- pid_lagrangian.py       # PID乘子更新器
|   |-- safe_sac.py            # 扩展SB3的SAC，加入Safety Critic + PID
|   +-- networks.py            # 网络定义
|-- config.py                   # 超参数
|-- train.py                    # 主训练脚本
+-- papers/                     # 参考文献
```

## 十、关键技术注意点

1. **目标重缩放** `1/(1+lambda)` 必须加上，防止lambda大时梯度爆炸
2. **微分项投影** `max(J_C - J_C_prev, 0)` 只在成本上升时激活
3. **Safety Critic 独立于 Reward Critic**，各用双Q网络
4. **按episode更新乘子**，不是每步更新
5. **使用非折扣episodic成本**作为PID控制器输入
6. **成本缩放 = 1/10**（论文 Table 1）
7. EV2Gym的cost.py需要改为返回过载代价（当前是经济成本）

## 十一、数据来源

| 数据 | 文件 | 来源 |
|------|------|------|
| 荷兰日前电价 | Netherlands_day-ahead-2015-2024.csv | ENTSO-E 公开数据 |
| 光伏出力 | pv_netherlands.csv | 荷兰 KNMI 气象数据 |
| 居民负荷 | residential_loads.csv | 公开负荷数据集 |
| EV到达分布 | distribution-of-arrival.csv | ELAAD 荷兰真实充电数据 |
| EV规格 | ev_specs.json | 多款真实车型参数 |

## 十二、实现工作量

| Phase | 内容 | 预估 |
|-------|------|------|
| 1 | 确认EV2Gym环境运行 + 理解数据结构 | 30min |
| 2 | 修改cost.py加入变压器过载代价 | 20min |
| 3 | 实现PID乘子更新器 (pid_lagrangian.py, ~30行) | 15min |
| 4 | 扩展SB3 SAC，加入Safety Critic + PID (~100行) | 1-2h |
| 5 | 编写训练脚本 + 配置 | 30min |
| 6 | 训练 + 调参 | 30-60min |
| 7 | 画图 (reward/cost/lambda 三条曲线) | 30min |